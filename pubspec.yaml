name: property
description: >-
  An observable value holder for Flutter. Update widgets without setState -
  as a ValueListenable, through a builder, or as a stream.
version: 1.0.0
homepage: https://github.com/Bomsamdi/property
repository: https://github.com/Bomsamdi/property
issue_tracker: https://github.com/Bomsamdi/property/issues

topics:
  - state-management
  - listenable
  - stream
  - widget

environment:
  sdk: ^3.8.0
  flutter: '>=3.32.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
